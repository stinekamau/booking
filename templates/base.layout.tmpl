{{define "base"}}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>

    {{block "css" .}}
        {{end}}

    {{block "js" .}}
        {{end}}
</head>
<body>
    {{block "content" .}}

        {{end}}
</body>
</html>
{{end}}